# Python deps for the data-generation venv (.venv-lfr, gitignored).
# Recreate with:
#   python3 -m venv .venv-lfr && ./.venv-lfr/bin/pip install -r lcdaGRASP/data-raw/requirements-venv.txt
#   ./.venv-lfr/bin/pip install torch --index-url https://download.pytorch.org/whl/cpu
# Used by: lfr_generate.py (canonical LFR), ecg_membership.py (ECG baseline),
#          gae_baseline.py (GNN baseline; needs torch + scikit-learn).
networkx>=3.6
igraph>=0.11
partition-igraph>=0.5
scikit-learn>=1.3
# torch (CPU) installed separately from the PyTorch CPU index (see above)
