% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_leaders.R
\name{lcda_plot_communities}
\alias{lcda_plot_communities}
\title{Detect communities and leaders on a graph, then plot them}
\usage{
lcda_plot_communities(
  graph,
  method = c("gr", "grasp", "ecg"),
  args = list(),
  seed = NA_integer_,
  plot = TRUE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{graph}{an [igraph::igraph] object (undirected, simple).}

\item{method}{which algorithm to run: `"gr"` ([lcda_gr()], the reactive
variant and the default), `"grasp"` ([lcda_grasp()]), or `"ecg"`
([lcda_ecg()], the ensemble consensus, which also yields node confidences).}

\item{args}{a named list of extra arguments for the chosen algorithm, e.g.
`list(B = 100, variant = 2)`.}

\item{seed}{integer RNG seed for the run, or `NA` to leave the RNG untouched.
Also seeds the layout, so the figure is reproducible.}

\item{plot}{logical; set to `FALSE` to fit and return without drawing.}

\item{verbose}{logical; passed to the algorithm.}

\item{...}{further arguments passed to [plot_partition()] (`layout`,
`legend`, `mark_communities`, ...).}
}
\value{
invisibly, the fitted result object (an `lcda_gr_result`,
  `lcda_grasp_result`, or `lcda_ecg_result`), with the layout used attached
  as the attribute `"lcda_layout"`. Feed it straight to [lcda_metrics()].
}
\description{
The one-call path from a bare graph to the paper's community-and-leader
figure: run one of the LCDA algorithms, take the partition and the leader it
designates for each community, and render them with [plot_partition()].
}
\examples{
g <- igraph::make_graph("Zachary")
res <- lcda_plot_communities(g, method = "grasp", args = list(B = 20), seed = 1)
lcda_metrics(res, level = "leader")
}
\seealso{
[lcda_metrics()], [plot_partition()].
}
