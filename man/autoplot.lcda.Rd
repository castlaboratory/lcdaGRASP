% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_leaders.R
\name{autoplot.lcda}
\alias{autoplot.lcda}
\alias{autoplot.lcda_grasp_result}
\alias{autoplot.lcda_gr_result}
\alias{autoplot.lcda_ecg_result}
\title{Community-and-leader map as a ggplot object}
\usage{
\method{autoplot}{lcda_grasp_result}(object, graph = NULL, layout = NULL, ...)

\method{autoplot}{lcda_gr_result}(object, graph = NULL, layout = NULL, ...)

\method{autoplot}{lcda_ecg_result}(object, graph = NULL, layout = NULL, ...)
}
\arguments{
\item{object}{a fitted `lcda_grasp_result`, `lcda_gr_result` or
`lcda_ecg_result`.}

\item{graph}{optional [igraph::igraph] to draw on; defaults to the graph
stored in `object`. Simplified before drawing, exactly as in
[plot.lcda_grasp_result()].}

\item{layout}{optional layout matrix or layout function; defaults to
Fruchterman-Reingold.}

\item{...}{further arguments passed to the internal renderer
(`leader_labels`, `palette`, `vertex_size`, `leader_size`, `title`).}
}
\value{
a `ggplot` object.
}
\description{
[ggplot2::autoplot()] methods for the fitted LCDA results: the same figure as
[plot.lcda_grasp_result()], returned as a `ggplot` object so it can be
themed, faceted, or saved with [ggplot2::ggsave()]. Requires the suggested
package \pkg{ggplot2}.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  g <- igraph::make_graph("Zachary")
  res <- lcda_grasp(g, B = 20, seed = 1)
  set.seed(1)
  ggplot2::autoplot(res)
}
}
\seealso{
[plot.lcda_grasp_result()], [lcda_plot_communities()].
}
