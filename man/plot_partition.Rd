% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_leaders.R
\name{plot_partition}
\alias{plot_partition}
\title{Plot a community partition with its leaders highlighted}
\usage{
plot_partition(
  g,
  membership,
  leaders,
  layout = NULL,
  leader_labels = TRUE,
  legend = TRUE,
  legend_max = 12L,
  vertex_size = 4,
  leader_size = 9,
  palette = "Dynamic",
  shade_edges = TRUE,
  mark_communities = FALSE,
  main = NULL,
  ...
)
}
\arguments{
\item{g}{an [igraph::igraph] object (undirected, simple).}

\item{membership}{integer vector of 1-based community ids, one per vertex.}

\item{leaders}{integer vector of 1-based leader vertex indices.}

\item{layout}{optional layout: a two-column matrix with one row per vertex,
or a layout function such as [igraph::layout_with_kk]. Defaults to
Fruchterman-Reingold. Pass an explicit layout for a reproducible figure.}

\item{leader_labels}{logical; label the leader vertices (with the vertex
`name` attribute when present, otherwise the vertex index).}

\item{legend}{logical; draw a community/leader legend. At most
`legend_max` communities are listed, with a "+k more" entry.}

\item{legend_max}{maximum number of communities listed in the legend.}

\item{vertex_size, leader_size}{plotting sizes for ordinary vertices and for
leaders.}

\item{palette}{an [grDevices::hcl.colors()] qualitative palette name.}

\item{shade_edges}{logical; colour intra-community edges by community and
grey out the inter-community ones.}

\item{mark_communities}{logical; additionally draw shaded hulls around the
communities (via `mark.groups`).}

\item{main}{plot title; `NULL` for a sensible default.}

\item{...}{further arguments passed to [igraph::plot.igraph()].}
}
\value{
invisibly, a list with the `layout` used and the `colors` per
  community (so a caller can reuse them for a companion figure).
}
\description{
Draws the graph with vertices coloured by community and the leader of each
community drawn larger, outlined, and labelled. Intra-community edges take
the community colour and inter-community edges are greyed, so the block
structure and the elected leaders are both readable at a glance. This is the
figure style used for the community-and-leader maps in Ospina et al. (2026).
}
\examples{
g <- igraph::make_graph("Zachary")
res <- lcda_grasp(g, B = 20, seed = 1)
set.seed(1)
plot_partition(g, res$best$membership, res$best$leaders)
}
\seealso{
[lcda_plot_communities()] for the end-to-end pipeline and
  [plot.lcda_grasp_result()] to plot a fitted result directly.
}
