% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lcda_grasp.R
\name{lcda_construct}
\alias{lcda_construct}
\title{LCDA construction - variant 1 (centrality computed once) or 2 (adaptive).}
\usage{
lcda_construct(
  csr,
  alpha_c,
  alpha_s,
  variant = 1,
  centrality = "eigen",
  similarity = "hpi",
  verbose = FALSE,
  cent = NULL
)
}
\arguments{
\item{csr}{CSR object from the internal converter.}

\item{alpha_c}{numeric in [0,1] - centrality RCL parameter.}

\item{alpha_s}{numeric in [0,1] - similarity RCL parameter.}

\item{variant}{1 (static centrality) or 2 (recomputed each iteration).}

\item{centrality}{one of "eigen", "betweenness", "closeness".}

\item{similarity}{one of "hpi", "dice", "jaccard".}

\item{verbose}{logical; emit a cli trace of the construction.}

\item{cent}{optional numeric vector of length `csr$n`: a precomputed global
centrality, used only by `variant = 1`. The global centrality is
deterministic per graph, so the outer GRASP loops compute it once and pass
it here instead of recomputing it at every one of the `B` iterations;
results are identical either way.}
}
\value{
list(membership, leaders, d) - membership a 1-based vector, leaders
  a 1-based integer vector of leader indices, d the number of communities.
}
\description{
LCDA construction - variant 1 (centrality computed once) or 2 (adaptive).
}
\examples{
g <- igraph::make_graph("Zachary")
csr <- as_csr(g)
sol <- lcda_construct(csr, alpha_c = 0.1, alpha_s = 0.3)
sol <- lcda_repair(csr, sol)
sol <- lcda_local_search(csr, sol)
c(communities = sol$d, leaders = length(sol$leaders))
}
\seealso{
[as_csr()] to build `csr`; [lcda_repair()] and [lcda_local_search()]
  for the remaining pipeline stages; [lcda_grasp()] for the all-in-one driver.
}
