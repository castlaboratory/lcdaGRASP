% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lcda_grasp.R
\name{lcda_gr}
\alias{lcda_gr}
\title{LCDA-GR: Reactive variant with self-tuning of (alpha_c, alpha_s).
Implements Algorithm 4 of the paper.}
\usage{
lcda_gr(
  g,
  variant = 1,
  B = 150,
  centrality = "eigen",
  similarity = "hpi",
  m = 20,
  y = NULL,
  alpha_c_range = c(0.1, 0.9),
  alpha_s_range = c(0.1, 0.5),
  p_floor = 0.05,
  verbose = FALSE,
  seed = NA_integer_
)
}
\arguments{
\item{g}{an igraph object (undirected, simple).}

\item{variant}{construction variant, 1 or 2.}

\item{B}{number of GRASP iterations.}

\item{centrality, similarity}{metric names.}

\item{m}{pool size (default 20, per paper).}

\item{y}{refresh period (default 3m).}

\item{alpha_c_range, alpha_s_range}{bounds for the uniform initial pool.}

\item{p_floor}{minimum probability mass reserved across the pool at each
refresh, spread uniformly so every pair keeps `p_k >= p_floor/m > 0`. This
prevents a pair that happened not to be sampled in a block from being
permanently excluded (and matches the `p_k >= delta > 0` premise of
Proposition 6). Set to 0 to recover the raw proportional rule.}

\item{verbose}{logical; show a cli progress bar and a final summary.}

\item{seed}{integer RNG seed, or `NA` to leave the RNG untouched.}
}
\value{
an object of class `lcda_gr_result`: best partition, traces, the
  reactive pool state, and the H-decisive iterations.
}
\description{
LCDA-GR: Reactive variant with self-tuning of (alpha_c, alpha_s).
Implements Algorithm 4 of the paper.
}
\examples{
g <- igraph::make_graph("Zachary")
res <- lcda_gr(g, B = 30, seed = 1)
res$best$Q
length(res$lex_decisive_iters)  # how often H broke a Q-tie
}
