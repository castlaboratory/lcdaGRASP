## Submission

New submission of lcdaGRASP 0.3.0.

## R CMD check results

Local check (macOS, R 4.6.0): 0 errors | 0 warnings | 2 notes.

* "New submission" — expected for a first submission.
* "Found the following (possibly) invalid URLs" — the GitHub repository and the
  pkgdown site URLs; these resolve once the repository is public and GitHub
  Pages is enabled.

The "checking HTML version of manual" NOTE seen locally is due to an outdated
local HTML Tidy and does not occur on CRAN's check machines.

## Test environments

* local: macOS, R 4.6.0
* win-builder (devel and release): pending
* R-hub (linux, windows, macos): pending

## Downstream dependencies

None — this is a new package.
